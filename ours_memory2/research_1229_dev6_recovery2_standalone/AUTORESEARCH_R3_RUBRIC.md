PASS only if all of the following hold:

1. The active `ours_memory2` mechanism and the cited local PReFine paper are mapped with concrete, line-addressable evidence, including generate-verify-refine, typed preference induction, and schema-level transfer.
2. The exact `1229_dev6` public task manifest, schemas, sanitized history, evaluator entry point, model snapshot, decoding budget, seeds, and primary metric are declared and hash-bound before any target-model call.
3. The append-only candidate revision, prompt text, routing policy, trait ontology, thresholds, tests, runtime contract, and baseline/candidate comparison plan are frozen before any target-model call. Earlier frozen lineage remains byte-identical.
4. No gold API call, gold argument, answer, target label/metadata, evaluator score/output, legacy prediction/latent output, or current-run model output is used to build memory, select a mechanism or prompt, tune a threshold, choose a variant, route an example, or construct another model call.
5. The current held-out query and public schema may enter only (a) the identical baseline/candidate final action prompt and (b) a frozen deterministic relevance/explicit-constraint gate at final inference. They must never enter memory generation, PReFine latent induction, ontology/threshold/prompt design, candidate selection, or any post-score adaptation. Baseline and candidate must receive byte-identical query/schema/action instructions; only the declared memory block may differ.
6. Gold stays unopened until both complete prediction files, provider journal, implementation/runtime manifests, and a pre-gold commitment are durably sealed and the model server is stopped. Gold is then read once through a same-file-descriptor, no-symlink path solely to compute the preregistered metrics.
7. Baseline and candidate cover exactly the same singleturn and multiturn cases with the same Qwen3-8B snapshot, tensor parallelism, seed schedule, decoding settings, retry policy, and one-call budget. Any missing/extra/duplicate call or parse failure is recorded and judged by the preregistration.
8. The predeclared primary aggregate and all non-regression, uncertainty/significance, coverage, parse-failure, and leakage gates are computed exactly once from the sealed outputs. Performance PASS requires the preregistered improvement; an honest miss is a valid research result but not mission PASS, and must not trigger a retuned same-test attempt.
9. Target-free structural diagnostics, focused boundary tests, full tests/static checks, commit-once state-machine tests, lineage checks, and independent data-provenance/methodology review all pass before evaluation. Failure analysis after evaluation may explain the sealed result but cannot authorize another target-model call under this mission.
10. Reproduction commands, exact hashes, environment/GPU/model identity, provider-call counts, artifacts, known limitations, and an independent strict critic PASS are recorded. No claim exceeds the evidence.
