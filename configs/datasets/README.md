# Dataset configs

Dataset configs identify source bytes and prepared-dataset versions. They must
record content hashes; large source data does not belong in this directory.

`mix600-v1.json` binds the external mix600 source hash, canonical small-input
config maps, expected G001 semantic digests, and the transitional safe output
directory `artifacts/prepared/mix600-v1`.

