{
  "affected_outputs": [
    "Core algorithm is the same; exp5 changes the default input root. Exp6 changes the default schema path and should be provenance metadata, not a paper-output label."
  ],
  "code_paths": [
    "methods/self_refine/experiments4/self_refine_preference_to_api.py",
    "methods/self_refine/experiments5/self_refine_preference_to_api.py"
  ],
  "distinction": "Core algorithm is the same; exp5 changes the default input root. Exp6 changes the default schema path and should be provenance metadata, not a paper-output label.",
  "schema": "experiments7-readable-variant/v1",
  "semantic_label": "self_refine_core45",
  "source_origins": [
    "experiments4",
    "experiments5"
  ],
  "stage": "support",
  "variant_id": "self_refine_core45"
}
